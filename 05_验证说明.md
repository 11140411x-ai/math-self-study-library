# 本版验证说明

检查日期：2026-09-16。

## 自动检查已通过

- 103 个模块唯一编号、必需字段及来源地址格式。
- 324个概念条目、103道例题、287道常规自测、18组高阶定理；另有12张深化卡的24道入门／迁移检测、8幅图解。
- 前置引用全部可解析，依赖图没有循环。
- Markdown 文件链接目标存在；合订本跨学段链接指向相应分册。
- HTML 内嵌全部正文、样式与脚本，不依赖外部字体或脚本服务。
- 24 项数值抽查通过，包括方差、分类离差、弦长、贝叶斯、泰勒误差界等。抽查并不等于对全部正文完成机器证明。

具体结构与数值记录见同目录“质量检查.json”。

v1.1另完成补充字段、重复题目、页面脚本语法检查，以及60项新增数值／参数代回抽查，见“深化验证.json”。新增81道分层题逐题复算并核对条件；这些检查用于找错，不构成对所有一般命题的形式化证明。重复构建的题数保持287，不会重复合并补丁。

## 内容复核

逐项审读了初高中例题、自测的答案及关键计算。另对 18 个高阶模块的条件、证明、18 道例题和 36 道自测做了独立审查；修正 A14 对零特征值与复数域情形的解释。未将“未发现错误”包装为永不出错的保证。

## 已实测的阅读器功能

v1.1在本机临时localhost服务上验证首页12个深化入口与新计数、J23入门答案展开、5道常规自测、讲解层级与图解显示；A07的逐步推理显示在正式定理前，15段原理／例题细解与5题正常显示；“自学审查”页面及表格能打开，未发现脚本错误。已查看勾股图实际截图并核对标注。下段为v1.0已有测试记录，不把旧测试混称为本轮全部重新实测。

在本机临时 localhost 服务中实际打开 HTML，观察首页和知识卡排版，验证：关键词搜索（泰勒返回 3 项）、高阶筛选（18 项）、无结果提示、学习路线跳转、高阶定理与推导显示、答案折叠展开、学习状态修改、笔记输入，以及刷新后状态和笔记仍保留。检查了 15 本资料清单及 10 条真题证据表。未发现页面脚本错误。

## 尚未完成的端到端检查

另外已验证两个同源标签页分别修改不同模块时，两条记录同时保留；普通保存会合并最新存储的当前字段，并监听其他标签页更新。相同模块同一字段的同时编辑仍采用后保存者为准，建议避免并行编辑同一条笔记。

本工具的内嵌浏览器限制直接访问 file URL，因此没有以工具自动完成“双击本地文件”的浏览器实测；已完成的浏览器检查使用 localhost，不需要你安装服务器才能使用最终 HTML。文件本身为自包含结构，推荐下载后用 Edge 或 Chrome 打开。

导出按钮已触发，但内嵌浏览器未返回可验证的下载事件，因此不声称“下载文件已经端到端验证通过”；导入文件选择与覆盖确认也未做真实文件往返测试。进度数据是本地记录，首次正式使用时建议先导出少量记录，再检查 JSON，并在备份副本上试导入。移动端断点已提供样式，但本轮未进行移动设备实测。

目标地区的历年题库、高中 2020—2025 课标完整逐条对照、教材逐页与每题校核仍未完成。详情在“来源与版本核查”和“真题证据与缺口”中明确保留。
