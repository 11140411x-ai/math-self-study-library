# 使用与维护

## 一次学习的建议流程

这是可调整的学习建议，不是为你测定的强制课表：先花几分钟检查前置与目标，再读概念和例题；遮住解析独立完成两道自测；最后用自己的话写一句“成立条件”和一句“易错点”。不能解释关键一步时，将状态设为“需复习”，到前置模块补上。

建议以“能独立解释和完成”为单位推进，不按一天刷完多少卡来判断效果。综合题与正式真题练习量仍需继续积累；卡片是知识入口和复习索引，不代替完整练习。

## 阅读页的进度

- 状态为“未学／学习中／需复习／已掌握”。推荐模块只根据你记录的状态及前置关系计算。
- 笔记和状态使用浏览器 localStorage。它不是云数据库；浏览器清理、隐私模式限制或文件位置变化可能影响保存。
- 页面会提示保存失败；发生失败时可先使用导出进度备份。导入会提示确认后覆盖当前这份库的进度与笔记，不修改教材与正文。
- 导出的 JSON 可能包含个人学习笔记。它由你自行保管；阅读页不会发送学习记录到外部服务器。

## 数学记号与条件

R、Z、N 分别为实数、整数、自然数，本库 N 默认包括 0；正整数写 N* 或明确 n≥1。Σ 表示求和，区间端点是否可取看圆／方括号。^ 表示乘方，例如 e^(0.2t)；Unicode 上下标也是数学记号，不是额外变量。

三角函数的求导、泰勒展开和复指数默认弧度。几何初等题写 ° 时用角度。向量夹角、直线夹角、指定二面角应区分：直线常取较小的角，二面角可能为钝角。

二项分布端点 p=0 或 p=1 按退化分布理解，不机械求 0⁰。分位数存在多种软件算法，J29 显式使用 np 位置规则，实际题目规定优先。描述方差用 n 分母，与推断中的无偏样本方差 n−1 分母不同。

## 证明层次

高阶知识卡的 theorem 字段注明“完整证明”所依赖的基础。这里“完整”是指在已列前提／基础定理之上把本卡结论推完，不表示从集合公理开始证明每条基础性质。

- A01 采用自然数良序。
- A02—A04 采用实数完备性；闭区间最值作为明示基础结果。
- A08 接受连续函数可积、积分线性及区间可加等基础性质；提供基本定理的核心证明。
- A10 对一般幂级数半径结构和逐项求导只给范围说明，没有展开一致收敛理论。
- A13 完整推导二阶逆矩阵；不声称完成一般行列式理论。
- A18 提及局部存在唯一性的标准条件但不展开证明；本卡分离公式与指数模型的证明已写出。

先在高中规定方法内解题，再用高阶工具回看结构。高阶内容属于拓展，不自动视作当地考试允许的直接援引结论。

## 维护方式

若只做个人笔记，使用阅读页笔记即可。若修改基础正文，优先编辑data下对应JSON；分层题与自学深化讲解编辑扩充资料下相应JSON，再运行构建工具。自动生成的知识卡与分册会被下一次构建覆盖，不能只改生成稿。正式修改前先备份整份ZIP，保留自己的新内容。

更新模块时保持 id 稳定，以免个人进度和前置链接失效。sources 中保留实际来源及“读到什么”的定位说明。题目若来自真实试卷，须另写年份、地区、卷别、题号、原卷链接及核对状态；不能去掉原创标记后直接称真题。

本版生成脚本只使用 Node.js 标准库，进入知识库目录后执行：

```powershell
node .\构建工具\build_kb.mjs
```

构建会读取本目录数据，并重建阅读页、知识卡、分册、索引及结构检查结果；不会访问云端，也不会自动上传。请在副本中试改并阅读检查结果。

图解索引与SVG已经随库提供。若修改图形生成代码，先运行 `node .\构建工具\build_diagrams.mjs`，再运行正文构建。详细讲解中的入门题与迁移题独立于practice数组统计，不能通过合并计数夸大常规题库数量。库版本更新到1.1不改变原进度格式，旧导出的进度JSON仍采用version=1。
